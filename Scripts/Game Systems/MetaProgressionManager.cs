// Created by Ömer Burak ÖZGÜR - Dungeon Janitors: Estate of Chaos project author
using UnityEngine;

/// <summary>
/// Manages persistent meta-progression systems such as upgrades, unlocks and currency.
/// Placeholder for future meta-progression implementation. // short
/// </summary>
public class MetaProgressionManager : SingletonManager<MetaProgressionManager>
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}
