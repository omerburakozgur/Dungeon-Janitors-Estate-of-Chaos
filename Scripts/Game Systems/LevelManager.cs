// Created by Ömer Burak ÖZGÜR - Dungeon Janitors: Estate of Chaos project author
using UnityEngine;

/// <summary>
/// Manages level-specific logic and global scene state transitions.
/// Currently a placeholder for future level management features (spawn control, streaming, checkpoints). // short
/// </summary>
public class LevelManager : SingletonManager<LevelManager>
{
    // Called once when the level manager initializes. Use for one-time setup. // short
    void Start()
    {
    }

    // Called every frame. Implement per-frame level updates here as needed. // short
    void Update()
    {
    }
}
